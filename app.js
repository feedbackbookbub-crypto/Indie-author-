async function runSearch(){const s=document.getElementById('status');s.textContent='SEARCH IN PROGRESS...';const platforms=[...document.getElementById('platforms').selectedOptions].map(x=>x.value);const body={platforms,genres:document.getElementById('genres').value.split(',').map(x=>x.trim()),synonyms:document.getElementById('synonyms').value.split(',').map(x=>x.trim()),state:document.getElementById('state').value,years:document.getElementById('years').value.split(',').map(Number),amount:Number(document.getElementById('amount').value)};const r=await fetch('/api/search',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(body)});const d=await r.json();s.textContent=`Generated ${d.queries.length} queries; collected ${d.results_count} results; found ${d.authors.length} authors.`;document.getElementById('rows').innerHTML=d.authors.map(a=>`<tr><td>${a.name}</td><td>${a.indie_score}%</td><td>${a.books.length}</td><td>${a.lead_score}</td><td>${a.category}</td></tr>`).join('')}